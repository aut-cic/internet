# Version control / CI
.git
.gitignore
.github

# Python virtualenv & caches (built inside the image)
.venv
__pycache__
**/__pycache__
*.py[cod]
.ruff_cache
.pytest_cache

# Node (installed inside the frontend stage) and stale build output
node_modules
frontend/node_modules
frontend/dist

# Local tooling / artifacts
.claude
.k6
*.tar.gz
prom/*.db

# Editor / OS noise
.DS_Store
